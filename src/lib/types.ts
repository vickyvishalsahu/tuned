export type Track = {
  id: string;
  title: string;
  artist: string;
  albumArtUrl: string;
  durationMs: number;
};

export type Quote = {
  text: string;
  author: string;
};

export type DominantColor = {
  r: number;
  g: number;
  b: number;
};
